<nav class="navbar sticky-top bg-white shadow-sm px-3 py-1">
    <!-- Brand: Logo + Text (Kiri) -->
    <div class="navbar-brand d-flex align-items-center">
        <img src="/images/Logo_Universitas_Diponegoro.png" height="60" draggable="false">
        <h1 class="brand-text fs-1 fw-bold">Rafika</h1>
    </div>

    <div class="d-flex align-items-center gap-2">
        <a href="/">
            <%- include('_buttonTransparent', {
                iconClass: 'bi-building',
                label: 'Fasilitas'
            }) %>
        </a>

        <% if (typeof user !== 'undefined' && user) { %>
            <!-- User sudah login -->
            <% if (user.role === 'pengguna') { %>
                <!-- Pengguna: Fasilitas, Laporan, Riwayat -->
            <% } else if (user.role === 'petugas') { %>
                <!-- Petugas: Tombol-tombol akan ditentukan dan ditambahkan nanti -->
            <% } else if (user.role === 'admin') { %>
                <!-- Admin: Tombol-tombol akan ditentukan dan ditambahkan nanti) -->
            <% } %>

            <!-- Tombol Keluar untuk semua role ketika logged in -->
        <% } else { %>
            <!-- Pengunjung (belum login): Fasilitas, Masuk, Daftar -->
                <a href="/masuk">
                <%- include('_buttonOutline', {
                    iconClass: 'bi-box-arrow-in-right',
                    label: 'Masuk'
                }) %>
            </a>

            <a href="/daftar">
                <%- include('_buttonSolid', {
                    iconClass: 'bi-person-plus-fill',
                    label: 'Daftar'
                }) %>
            </a>
        <% } %>
    </div>
</nav>