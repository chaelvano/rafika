# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/rafika

# Session Secret (generate via: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your-secret-key-min-32-characters-here

# Node Environment
NODE_ENV=development

# Server Port
PORT=3000
